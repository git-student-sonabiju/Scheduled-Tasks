spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/scheduled-tasks
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: create
    show-sql: true
